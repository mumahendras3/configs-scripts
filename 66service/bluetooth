[main]
@type = longrun
@name = bluetooth
@description = "Service for running the BlueZ daemon"
@user = (root)
@depends = (dbus)
@options = (log)

[start]
@build = auto
@execute = (
if { s6-echo -- Starting BlueZ daemon }
/usr/lib/bluetooth/bluetoothd -n
)
